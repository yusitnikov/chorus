const container = typeof window !== "undefined" ? window : {};

export const ks = container.config?.ks;
export const Kaltura = container.Kaltura;
export const KalturaPlayer = container.KalturaPlayer;
export const fixWebmDuration = container.ysFixWebmDuration;
